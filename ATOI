#include <stdio.h>
int Toint(char* string)
{
	int output=0;
	for (int i = 0; string[i] != '\0'; ++i)
	{
		output = output * 10 + string[i] - '0';
	}
	printf("using Atoi function;  %d ", output);
}
int main()
{
	char string[] = "1234567890";
	Toint(string);//not using built in 
	return 0;
}
